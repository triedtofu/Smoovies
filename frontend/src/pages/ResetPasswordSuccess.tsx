import React from 'react';

import MakePage from '../components/MakePage';

const ResetPasswordSuccess = () => {
  return (
    <h1>An email with a link has been sent to your registered email account</h1>
  );
};

export default MakePage(ResetPasswordSuccess);
